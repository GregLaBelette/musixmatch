<h1 class="mb-4">My songs</h1>

<ul>
  <% @songs.each do |song| %>
    <%= link_to song_path(song) do %>
      <li><%= song.title %> by <strong><%= song.artist %></strong></li>
    <% end %>
  <% end %>
</ul>
